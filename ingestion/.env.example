# Copy this to .env (or export in your shell) and adjust values to match your setup.
# .env itself is gitignored — do not commit credentials.

DB_URL=postgresql://postgres:postgres@localhost:5432/coffee_sme
DATA_DIR=../mock_data
